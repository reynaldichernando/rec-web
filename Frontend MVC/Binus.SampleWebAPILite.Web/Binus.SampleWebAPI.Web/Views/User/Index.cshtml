@model Binus.SampleWebAPI.Web.ViewModels.UserViewModel
@{
    var Request = HttpContext.Current.Request;
    string Address = Request.Url.Scheme + "://" + Request.Url.Authority + Request.ApplicationPath.TrimEnd('/');
    ViewBag.Title = "User";
}

@*<table class="table table-bordered">
    <thead>
        <tr>
            <td>Name</td>
            <td>Email</td>
            <td>Role</td>
            <td>Action</td>
        </tr>
    </thead>
    <tbody>
        @foreach (var Data in Model.ListUser)
        {
            <tr>
                <td>@Data.Name</td>
                <td>@Data.Email</td>
                <td>@Data.Role</td>
                <td>
                    @Html.ActionLink("Verify", "VerifyUser", Data)
                </td>
            </tr>
        }
    </tbody>
</table>*@

<div class="layout-header p-3">
    <h1 class="text-white"><i>Profile</i></h1>
</div>

<div class="layout-content p-5 text-center h-100">
    <div class="m-3">
        <h3>Nama</h3>
        <p>Blabla</p>
    </div>
    <div class="m-3">
        <h3>Email</h3>
        <p>a.a@aa</p>
    </div>
    <button type="submit" class="m-3">Verify User</button>
</div>

<footer class="footer">
    <div class="container">
        <span class="text">&copy; 2020 Copyright: Kelompok 1</span>
    </div>
</footer>