@model Binus.SampleWebAPI.Web.ViewModels.ScheduleViewModel
@{

    ViewBag.Title = "Index";
}

<!DOCTYPE html>

<html>
<head>
    <script type="text/javascript" src="/scripts/jquery.min.js"></script>
    <script type="text/javascript" src="/scripts/moment.min.js"></script>
    <script type="text/javascript" src="/scripts/bootstrap.min.js"></script>
</head>

<body>
    <div class="split right border border-dark">
        <div class="centered">
            @using (Html.BeginForm("ChangePassword", "Password", "POST")) {
                <input type="hidden" class="form-control mb-2" name="hash" value="">
                <input type="text" id="inputEmail" class="form-control mb-2" name="Email" placeholder="Email" value="@HttpContext.Current.Session["Email"].ToString()">
                <label for="inputPassword" class="sr-only">Password</label>
                <input type="password" id="inputPassword" class="form-control mb-2" name="Password" placeholder="Password" required>
                <span class="text-danger" style="display: none" id="error-empty-password">
                    Please input
                    password <br>
                </span>
                <label for="inputConfirmPassword" class="sr-only">Confirm Password</label>
                <input type="password" id="inputConfirmPassword" class="form-control mb-2" name="ConfirmPassword" placeholder="Confirm Password" required>
                <span class="text-danger" style="display: none" id="error-confirm-password">
                    Please correctly confirm the
                    password <br>
                </span>

                <span id="divMessage" style="color:red"></span>
                <button class="btn btn-lg btn-primary btn-block" id="btnLoginDB" type="submit">Send request</button>
            }

        </div>
    </div>




    <div class="wrapper">

        @if (Session["Role"].Equals("admin")) {
            @Html.ActionLink("Insert Schedule", "", "Schedule", new { }, new { @class = "btn btn-info", data_toggle = "modal", data_target = "#insertModal" });
        }

    </div>
    <table class="table table-bordered">
        <thead>
            <tr>
                <td>Book Title</td>
                <td>Book Desc</td>
                <td>Book Qty</td>
                <td>Start time</td>
                <td>End time</td>

                @if (Session["Role"].Equals("admin")) {
                    <td>Action</td>
                }
            </tr>
        </thead>
        <tbody>

            @foreach (var Data in Model.ListSchedule) {
                <tr>
                    <td>@Data.ScheduleID</td>
                    <td>@Data.Place</td>
                    <td>@Data.Topic</td>
                    <td>@Data.StartTime</td>
                    <td>@Data.EndTime</td>

                    @if (Session["Role"].Equals("admin")) {
                        <td>
                            <button onclick="Delete(@Data.ScheduleID)" on class="btn btn-danger">Delete</button>
                            @Html.ActionLink("Update Schedule", "", "Schedule", new { }, new { @class = "btn btn-info", data_toggle = "modal", data_target = "#updateModal", data_id = Data.ScheduleID })
                        </td>
                    }

                </tr>
            }

        </tbody>
    </table>


    @* Insert *@
    @using (Ajax.BeginForm("CreateSchedule", "Schedule", new AjaxOptions {
        HttpMethod = "POST",
        OnSuccess = "LoadResult(data)"
    }, new { id = "form-create-schedule", enctype = "multipart/form-data" })) {
        @Html.AntiForgeryToken()

        <!-- Modal -->
        <div id="insertModal" class="modal fade" role="dialog">
            <div class="modal-dialog modal-lg">
                <!-- Modal content-->
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                        <h4 class="modal-title">Modal Header</h4>
                    </div>
                    <div class="modal-body">
                        <div class="row Data">
                            <div class="col-sm-12">
                                <div class="alert alert-info alert-dismissable" id="iMessageContainer">
                                    <button aria-hidden="true" data-dismiss="alert" class="close" type="button">×</button>
                                    <span id="Message"></span>
                                </div>
                                <input type="hidden" id="itxtScheduleID" name="ScheduleID" />
                                <div class="form-group">
                                    <label for="txtTopic">Topic:</label>
                                    <input type="text" class="form-control input-lg" required id="itxtTopic" name="Topic">
                                </div>
                                <div class="form-group">
                                    <label for="txtDesc">Description:</label>
                                    <input type="text" class="form-control input-lg" required id="itxtDesc" name="Description">
                                </div>
                                <div class="form-group">
                                    <label for="txtPlace">Place:</label>
                                    <input type="text" class="form-control input-lg" required id="itxtPlace" name="Place">
                                </div>
                                <div class="form-group">
                                    <label for="txtStartTime">Start Time:</label>
                                    <input type="datetime-local" class="form-control input-lg" required id="itxtStartTime" name="StartTime">
                                </div>
                                <div class="form-group">
                                    <label for="txtEndTime">End Time:</label>
                                    <input type="datetime-local" class="form-control input-lg" required id="itxtEndTime" name="EndTime">
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="submit" id="submit-schedule" class="btn btn-danger">Submit</button>
                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    </div>
                </div>

            </div>
        </div>
    }



    @* Update *@
    @using (Ajax.BeginForm("UpdateSchedule", "Schedule", new AjaxOptions {
        HttpMethod = "POST",
        OnSuccess = "LoadResult(data)"
    }, new { id = "form-create-schedule", enctype = "multipart/form-data" })) {
        @Html.AntiForgeryToken()

        <!-- Modal -->
        <div id="updateModal" class="modal fade" role="dialog">
            <div class="modal-dialog modal-lg">
                <!-- Modal content-->
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                        <h4 class="modal-title">Modal Header</h4>
                    </div>
                    <div class="modal-body">
                        <div class="row Data">
                            <div class="col-sm-12">
                                <div class="alert alert-info alert-dismissable" id="MessageContainer">
                                    <button aria-hidden="true" data-dismiss="alert" class="close" type="button">×</button>
                                    <span id="Message"></span>
                                </div>
                                <input type="hidden" id="txtScheduleID" name="ScheduleID" />
                                <div class="form-group">
                                    <label for="txtTopic">Topic:</label>
                                    <input type="text" class="form-control input-lg" required id="txtTopic" name="Topic">
                                </div>
                                <div class="form-group">
                                    <label for="txtDesc">Description:</label>
                                    <input type="text" class="form-control input-lg" required id="txtDesc" name="Description">
                                </div>
                                <div class="form-group">
                                    <label for="txtPlace">Place:</label>
                                    <input type="text" class="form-control input-lg" required id="txtPlace" name="Place">
                                </div>
                                <div class="form-group">
                                    <label for="txtStartTime">Start Time:</label>
                                    <input type="datetime-local" class="form-control input-lg" required id="txtStartTime" name="StartTime">
                                </div>
                                <div class="form-group">
                                    <label for="txtEndTime">End Time:</label>
                                    <input type="datetime-local" class="form-control input-lg" required id="txtEndTime" name="EndTime">
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="submit" id="update-schedule" class="btn btn-danger">Submit</button>
                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    </div>
                </div>

            </div>
        </div>
    }




    <script src="~/Sources/PageJS/Backend/schedule.js"></script>
</body>

</html>
